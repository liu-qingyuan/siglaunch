# Siglaunch

A local-first macOS hub for gesture-triggered developer workflows.

## Development

Siglaunch requires macOS 13 or newer and Swift 6.

```bash
swift run Siglaunch
```

Run the coordinator behavior tests with:

```bash
swift test
```
